# Local Password Manager

A simple local password manager with a web interface accessible at `localhost:3000`.

## Features

* Store and manage passwords locally
* Browser-based interface
* No cloud or external server required

## Usage

Start the server and open `http://localhost:3000` in your browser.
